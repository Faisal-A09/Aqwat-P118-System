# Aqwat sync status

The submitted ZIP was compared against the GitHub Pages repository.

Confirmed mismatch from live site screenshot:
- The live site still showed the old logo next to Aqwat.

Submitted file state:
- `index.html` header removes the logo block from `.brand`.
- The gate icon remains intentionally inside the access dialog.

Note: GitHub Pages can cache old assets briefly. After updating, hard-refresh the browser or clear site data.
